---
description: Backend Express/Prisma — API and database conventions for finance-dashboard.
globs: backend/**
alwaysApply: false
---

# Backend

- Models and migrations: `backend/prisma/`; HTTP logic in `backend/src/app.ts`.
- When serializing amounts and currencies, follow patterns in `app.ts` (`toNumber`, `normalizeCurrency`).
- FX rates: `backend/src/fx.ts` — do not duplicate FX logic in route handlers.
- REST endpoints under `/api/...`; JSON responses; appropriate HTTP status codes on errors.
- Secrets only in `.env` (do not commit); `dotenv` loads configuration at startup.
